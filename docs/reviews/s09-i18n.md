# Revue — Story s09-i18n

Contexte frais. Diff jugé : `git show bd7abe0`. Branche `dev`, base propre au début
et à la fin (`git diff --exit-code` vert avant l'écriture de ce fichier).

## Ce qui a été exécuté, par moi

| Commande | État livré (i18n activé) | `pnpm ks toggle i18n` (coupé) |
|---|---|---|
| `pnpm typecheck` | vert | vert |
| `pnpm lint` | vert (`--max-warnings=0`) | vert |
| `pnpm test` | **641 passés, 2 ignorés** (25 fichiers + 1 ignoré) | **641 passés, 2 ignorés** |
| `pnpm test:e2e` | **26 passés**, deux exécutions consécutives, `retries: 0` | **24 passés, 2 ignorés** |
| `pnpm build` | vert — arborescence de routes **sans `[locale]`** | vert — **même** arborescence |
| `pnpm run audit` | vert (1 avis, aucun au seuil « élevé » non excepté) | — |

Le module a été remis en marche (`pnpm ks toggle i18n`) et l'arbre revérifié propre.

Écrans regardés dans un navigateur réel (serveur de production `next start`, pas
seulement `next dev`) : accueil, connexion, inscription, mot de passe oublié,
compte connecté — en `fr` et en `en`, en 1280 px et en 380 px, clair et sombre.

---

## 1. Le cœur de la story : « aucune chaîne en dur » — **critical**

Le plan (tâche 8) écrit : « Il doit détecter un **texte affiché qui ne vient pas
des catalogues**. » Le critère 3 de `docs/stories.md` écrit : « un test échoue si
un texte affiché ne provient pas des fichiers de traduction. »

J'ai planté quatre formes, ailleurs et autrement que l'implémenteur, et mesuré.

| Plant (dans `apps/web/app/page.tsx`, rendu à l'écran) | Résultat |
|---|---|
| `<h2>Bienvenue sur votre espace</h2>` (le témoin, sa forme) | **rouge** — 1 test, nommant le fichier et le texte |
| ``<h2>{`Bienvenue sur votre espace ${account.name}`}</h2>`` | **vert** — `test`, `lint`, `typecheck` : rien |
| `title={'Bonjour ' + account.name}` (concaténation) | **vert** |
| `label: 'Mon espace personnel'` dans `apps/web/lib/navigation.ts` (`.ts`) | rouge, mais par `tests/app-shell.test.ts`, pas par le détecteur |

Deux causes, dans `tests/i18n.test.ts` :

- `displayedStringsOf` n'extrait que `/(['"])…\1/` — **l'accent grave n'est pas
  dans la classe**. Un littéral gabarit est invisible, et un nœud JSX qui en
  contient un l'est aussi (`jsxText` exclut tout ce qui porte `{`) ;
- `PROSE = /[À-ÿ]|(?:\p{L}{2,}[ ]\p{L}{2,})/u` demande **deux mots ou un
  accent** : `'Bonjour '` seul n'est ni l'un ni l'autre, donc une phrase coupée
  en morceaux d'un mot passe.

C'est le mode d'échec n°4 du catalogue de `docs/STATE.md` — « garde textuelle
contournée par un guillemet, **un accent grave**, une extension, un paquet
unifié — s07, s08 » — reparu sur le livrable qui **est** la story. Et la règle du
`AGENTS.md` racine s'applique telle quelle : « A green mutation means the test is
wrong, not that the code is right. »

Aucun texte en dur de cette forme n'existe aujourd'hui — j'ai balayé les 30
`.tsx` du dépôt, aucun littéral gabarit ne porte de prose. Le défaut n'est donc
pas dans le produit livré : il est dans le filet. Mais ce filet est ce contre quoi
les trente-six stories suivantes seront écrites, et la manière naturelle
d'écrire un texte interpolé en TSX est précisément un littéral gabarit.

Ce qui reste vrai et solide, pour ne pas le perdre : le détecteur balaie
réellement (30 fichiers `.tsx` sur 30, mesuré), il n'inventorie pas les
catalogues, il nomme le fichier et le texte quand il mord, et sa moitié
« présence » mord aussi — j'ai retiré `app.dashboard.anonymous.title` de
`apps/web/messages/en.json` : **2 tests rouges**.

**Correctif attendu** : ajouter l'accent grave à la classe de guillemets (en
gardant les segments de gabarit hors des `${…}`), et faire mordre `PROSE` sur un
fragment d'un seul mot suivi ou précédé d'une concaténation. Puis rejouer les
quatre plants ci-dessus.

---

## 2. Tâche 1 — la forme des routes : **vérifiée, le constat tient**

L'implémenteur affirme que dans `next-intl@4.14.1`, seul `next-intl/middleware`
impose un segment `[locale]`. Vérifié dans le paquet installé :

- `getLocaleAsPrefix` n'apparaît que dans `dist/*/middleware/*`,
  `navigation/react-client/useBasePathname.js` et `shared/utils.js` — jamais dans
  `server/react-server/getConfig.js` ;
- `dist/types/server/react-server/getRequestConfig.d.ts` documente lui-même le cas
  « a page outside of the `[locale]` segment renders (e.g. `app/page.tsx`) », et
  `requestLocale` n'est pas lu par `apps/web/i18n/request.ts` ;
- le greffon `createNextIntlPlugin` ne fait qu'aliaser `next-intl/config`.

Le manifeste de build le confirme dans **les deux états** : `/`, `/account`,
`/sign-in`, `/sign-up`, `/forgot-password`, `/reset-password`, `/verify-email`,
`/api/health`, `/api/modules/[...path]` — **aucun `[locale]`**, et `ƒ Proxy
(Middleware)` monté.

Critère 8 mesuré pour de vrai, module coupé, sur un serveur démarré :

- `GET /` → **200**, aucune redirection, aucun `Set-Cookie` de locale ;
- les mêmes URL servent qu'avec le module (`e2e/i18n.spec.ts:24` dérive ses
  attentes de `localeRouting`, il ne les recopie pas) ;
- 24 parcours verts, 2 ignorés par `test.skip(servesOneLanguage)` — les deux qui
  n'ont pas de sens à une seule langue.

Et module activé : `GET /` avec `Accept-Language: en` → `307 → /en` ;
`GET /fr/account` anonyme → `307 → /fr/sign-in?next=%2Faccount` (le `next` porte
le chemin **interne**, une seule mise en forme publique) ; `GET /api/health` →
200 sans préfixe.

Mutations sur les invariants de cet axe, toutes **rouges** :

| Neutralisation | Rouges |
|---|---|
| `resolveLocale` sans la garde `locales.includes(candidate)` | 3 |
| `singleLocaleRouting.canonicalPath` rendant une redirection | 1 |
| `publicPath` privé de son idempotence (le bug `/fr/fr`) | 1 |
| `localeOptions` privé de sa garde `locales.length < 2` (le 500 module coupé) | 1 |
| `assertDeclarationsAreComplete` contrôlant contre les locales du module | 3 |
| `readRequestLocale` rendant toujours la locale du site | 1 parcours e2e |

Les deux défauts trouvés au navigateur sont donc réellement tenus par des tests,
et les tests mordent.

**Chasse à la même classe** — « ce qui ne se voit qu'au navigateur ». Le cas du
500 était : un écran demande une clé appartenant à un module absent. Les sept
écrans livrés sont tous réellement rendus dans les deux états par la suite
Playwright (`/`, `/sign-in`, `/sign-up`, `/forgot-password`, `/verify-email` par
`i18n.spec.ts`, `/account` et `/reset-password` par `auth.spec.ts` et
`app-shell.spec.ts`). La classe est fermée pour les écrans d'aujourd'hui. Elle
n'est pas fermée par construction : `tests/i18n.test.ts` contrôle les clés citées
contre l'annuaire **complet** — délibérément, et l'argument est bon — donc une
clé de module coupé citée sans garde ne rougirait qu'en e2e, sur l'écran
concerné.

---

## 3. Le point structurel signalé par l'implémenteur — **acceptable, avec une réserve**

`apps/web/lib/locale-routing.ts` connaît `@repo/module-i18n` par son nom et
demande au registre s'il est monté. Jugement : **acceptable**.

- La branche est confinée à un fichier, et ce qu'elle rend est une interface
  possédée par `@repo/core` (`LocaleRouting`) — les appelants restent ignorants.
  Vérifié : `moduleRegistry.moduleIds.includes(i18nModule.id)` est la **seule**
  occurrence de `@repo/module-i18n` hors `config/features.ts`, `next.config.ts` et
  les tests ;
- l'identifiant vient du module lui-même (`i18nModule.id`), pas d'une chaîne
  recopiée qu'un renommage laisserait fausse ;
- l'alternative — une capacité de routage au contrat — est explicitement rejetée
  par l'ADR 007 (« ajouter `purge`, `export` ou `retention` après vingt modules
  obligerait à tous les rouvrir »). Le point de composition est le bon endroit.

La réserve, et c'est ce qui décide s'il se multipliera : ce n'est **pas** la même
situation que `lib/auth.ts`. `auth` est dans `requiredModules`, donc ce fichier
n'a aucune condition à porter ; `lib/mailer.ts` choisit sur une configuration, pas
sur un identifiant de module. `lib/locale-routing.ts` est le **premier** fichier
qui branche sur l'existence d'un module optionnel. La règle « aucune branche
ailleurs » est écrite dans `apps/web/AGENTS.md` et dans
`packages/modules/i18n/AGENTS.md`, mais **aucune commande ne la vérifie** :
`eslint.config.ts` restreint les imports d'application et de Radix, jamais
`@repo/module-*` depuis `apps/web/app/**`. C'est le test du `AGENTS.md` racine —
« which command fails if I break it ? » — sans réponse. Voir finding m2.

---

## 4. Locale des emails — **critère 5 tenu, critère 6 tenu, avec une dette nommée**

Câblage vérifié dans le paquet installé, pas dans la documentation :
`better-auth@1.7.2` appelle `sendResetPassword(data, ctx.request)`
(`dist/api/routes/password.mjs:83`) et `sendMagicLink(data, ctx)`
(`dist/plugins/magic-link/index.mjs:93`) — les deux signatures que
`better-auth-service.ts` exploite existent bien.

Le chemin est unique : `emailLocaleFor` → `resolveLocale` (`@repo/core`), la
**même** fonction que l'écran. Aucun second chemin d'envoi ni de rendu ; le port
`Mailer` et `@repo/emails` sont inchangés. Interdit respecté.

Preuve end to end refaite : navigateur `en-GB`, inscription, lecture de la boîte
capturée — sujet et corps anglais présents, la version française **absente**. Et
la mutation le confirme : `readRequestLocale` rendu constant → ce parcours
**rouge**, les cinq autres verts.

Jugement sur « pas de locale stockée » (pas de colonne `auth_user.locale`) : c'est
**suffisant aujourd'hui** — les quatre envois du module partent vers la personne
qui vient de faire la requête, y compris le changement d'adresse, dont le
destinataire est la nouvelle adresse de ce même utilisateur. Et c'est
**insuffisant demain**, de deux façons qui ne sont écrites nulle part :

1. un email non déclenché par une requête du destinataire (tâche planifiée, purge
   s33, relance de paiement sur webhook Stripe) n'a aucune requête à lire et
   partira dans la locale du site, en silence — le critère 5 dit « la langue de
   l'utilisateur destinataire » ;
2. l'idiome `knownLocale: auth.localeOf(request)` est répété quatre fois dans
   `auth-routes.ts`. Recopié pour une **invitation** (s15), il enverrait
   l'invitation dans la langue de **l'invitant**, pas dans celle du site que le
   critère 6 impose.

`packages/modules/auth/AGENTS.md` ne dit rien de tout ça. Voir finding m3.

---

## 5. Les déviations à juger

- **`packages/core/src/i18n.ts`** — justifiée. Le précédent invoqué
  (`protection.ts`) tient : règle lue par des appelants sans rien en commun, et
  `singleLocaleRouting` doit exister **quand le paquet du module n'est pas dans le
  dépôt**. Fichier pur, sans framework ni ORM. Réexports du baril vérifiés un à un.
- **`tests/module-off.test.ts`, égalité → inclusion** — correcte. La direction
  conservée (« aucune route ne vient d'un module non activé ») est celle qui porte
  la sécurité ; celle qui a sauté (« tout module activé monte une route ») n'est
  écrite nulle part et `i18n` la contredit légitimement. Le garde-fou
  `mounted.size > 0` empêche le cas vide.
- **`playwright.config.ts`, `locale: 'fr-FR'`** — **nécessaire, et je l'ai mesuré** :
  ligne retirée, `pnpm test:e2e` tombe à **7 passés / 19 échoués**. L'épingle est
  porteuse ; sans elle la suite passait ou non selon la machine, exactement la
  non-détermination que `retries: 0` existe pour exposer.
- **`mailSentTo` extrait de `linkSentTo`** — correct, et la raison écrite est la
  bonne : le texte affirmé et le lien suivi viennent du même courrier.
- **Le bug de `test()` sur une expression régulière globale** — vrai bug, vrai
  correctif : `LINK_PATTERN` porte `/g`, `RegExp.prototype.test` reprend à
  `lastIndex` et rend faux un appel sur deux ; `String.prototype.match` avec `/g`
  ignore `lastIndex`. Le commentaire dit exactement cela.
- **`apps/web/AGENTS.md`** — amendement honnête et borné : il **nomme** la seconde
  exception au lieu de la taire, et les deux « mesuré dans le paquet installé »
  qu'il avance sont exacts (je les ai revérifiés).

---

## Findings

### critical

**C1 — Le test « aucune chaîne en dur » ne détecte ni un littéral gabarit ni une
phrase concaténée.** `tests/i18n.test.ts`, `displayedStringsOf` et `PROSE`.
Planté ``<h2>{`Bienvenue sur votre espace ${account.name}`}</h2>`` dans
`apps/web/app/page.tsx` : `pnpm test` **641 verts**, `pnpm lint` vert, `pnpm
typecheck` vert. Idem pour `title={'Bonjour ' + account.name}`. Le critère 3 de
`docs/stories.md` n'est donc pas tenu pour ces deux formes, et c'est le mode
d'échec n°4 déjà catalogué dans `docs/STATE.md`. Aucune chaîne de cette forme
n'existe dans le dépôt aujourd'hui (30 `.tsx` balayés, 0 trouvée) : c'est le filet
qui est en cause, pas le produit — mais ce filet est ce sur quoi s10 à s45
reposent.

### major

**M1 — La moitié « jamais silencieusement remplacée en production » du critère 8
n'est tenue par rien qui morde.** Le seul contrôle est une expression régulière
sur le texte source (`tests/i18n.test.ts`, « refuse une clé manquante plutôt que
de la remplacer par elle-même ») :
`expect(source).toMatch(/onError:[\s\S]*?throw/)`. Deux neutralisations, **les
deux vertes** : (a) `onError` remplacé par `console.error(error)` — le `[\s\S]*?`
non gourmand se satisfait du `throw` de `getMessageFallback`, plus loin dans le
fichier ; (b) `getMessageFallback` renvoyant le chemin de la clé, avec le `throw`
laissé derrière, inatteignable. Le code de production est juste (vérifié dans
`use-intl@4.14.1` : `d()` appelle `onError` avant tout repli, donc lever
propage) ; c'est l'affirmation « éprouvée ici sur la seule chose qu'un test de
nœud peut observer sans navigateur » qui est fausse — `createTranslator` de
`use-intl` s'appelle depuis un test de nœud, avec un catalogue amputé.

**M2 — Le cookie `app_locale` est posé sans `HttpOnly`.** `apps/web/proxy.ts:70`.
Mesuré sur le serveur : `set-cookie: app_locale=en; Path=/; Max-Age=31536000;
Secure; SameSite=lax`. `docs/security.md` §1 pose la règle sans condition
(« Cookies | `HttpOnly`, `Secure`, `SameSite=Lax` au minimum »), et nomme la
commande qui doit la tenir (« test sur l'en-tête `Set-Cookie` ») : ce test
n'existe pour aucun cookie hors session. Rien côté client ne lit ni n'écrit ce
cookie — le sélecteur est une liste de liens, le proxy l'écrit côté serveur —,
donc `httpOnly: true` ne coûte rien. Impact de confidentialité négligeable (une
préférence d'affichage, aucun privilège) : c'est pour ça que ce n'est pas classé
critical, mais c'est bien un écart au socle non négociable, et le premier cookie
non-session du dépôt fixe le précédent pour les suivants.

**M3 — Le contrôle des locales de l'application n'est pas exécutable à son point
de composition.** `buildRegistry({ locales? })` est **facultatif** et retombe en
silence sur les locales du module. Mutation : `locales: [...appLocales]` retiré de
`apps/web/lib/module-registry.ts` — c'est-à-dire le retour exact à la faille de
s06 que la tâche 2 existe pour fermer — `pnpm test` **641 verts**, `lint` vert,
`typecheck` vert. Le repli optionnel est justifié par les fixtures de test, mais
alors rien ne garantit que les points de composition le passent : les trois
d'aujourd'hui (`apps/web`, `db:generate`, `db:migrate`) le font, un quatrième
(seed, serveur MCP de s41) ne le fera pas et personne ne le saura.

### minor

**m1** — `packages/modules/i18n/AGENTS.md` désigne `apps/web/lib/i18n.ts` comme
« le montage des deux états ». Le choix vit dans `apps/web/lib/locale-routing.ts` ;
`lib/i18n.ts` ne fait que résoudre. `apps/web/AGENTS.md` dit juste. Un agent qui
lit le `AGENTS.md` du paquet est envoyé au mauvais fichier.

**m2** — Aucune règle exécutable n'empêche un écran d'importer `@repo/module-i18n`
ni d'écrire `moduleRegistry.moduleIds.includes('i18n')`. L'interdit « aucune
branche sur l'existence de l'i18n » vit dans deux `AGENTS.md` et nulle part
ailleurs. Candidat : un `no-restricted-imports` sur `@repo/module-*` pour
`apps/web/**` avec `lib/auth.ts` et `lib/locale-routing.ts` en `ignores`.

**m3** — La dette « pas de locale stockée » n'est écrite nulle part où le
prochain agent la lira. Ni `packages/modules/auth/AGENTS.md` ni
`apps/web/AGENTS.md` ne disent que `localeOf(request)` désigne **le demandeur**,
donc qu'il ne doit pas servir pour un destinataire tiers (invitation s15), et
qu'un envoi hors requête (tâche, webhook) n'a par construction aucune langue
connue.

**m4** — Le sélecteur renvoie toujours à `/<locale>` (`localeOptions` :
`routing.publicPath('/', candidate)`). Changer de langue depuis `/fr/account`
ramène à l'accueil anglais et perd le contexte. Comportement délibéré et testé,
mais c'est une régression d'usage face à un sélecteur qui reste sur place.

**m5** — `tests/i18n.test.ts` justifie de ne balayer que les `.tsx` par « Un `.ts`
n'affiche rien — ses chaînes sont des messages de journal, des raisons d'erreur
JSON ou des textes d'email déclarés au contrat, et chacun a sa propre garde ».
`apps/web/lib/navigation.ts`, dans le même commit, calcule des `label` affichés.
C'est l'affirmation d'exhaustivité que le `AGENTS.md` racine interdit ; la forme
mesurée serait « les 30 `.tsx` du dépôt, et voici pourquoi les `.ts` d'aujourd'hui
sont couverts ailleurs ».

### Observation — pas un finding contre s09

Les écrans d'authentification (`/sign-in`, `/sign-up`, `/forgot-password`,
`/reset-password`, `/verify-email`) sont rendus **sans le design system** :
`auth-form.tsx` produit des `<form>/<p>/<label>/<input>/<button>` nus, sans classe,
et `<h1>` s'affiche à 14 px. Vu au navigateur en build de production, dans les
deux langues et les deux thèmes. Vérifié dans `git show 5bf3322` : c'est l'état
**antérieur** à s09, et l'interdit de run (« ne pas réécrire les écrans de s07 et
s08 : les traduire ») commandait de ne pas y toucher. L'implémenteur a bien fait.
Cela mérite sa propre story : ces cinq écrans sont les premiers que voit un
acheteur du boilerplate. Les écrans qui utilisent le design system (accueil,
compte) sont, eux, corrects en `fr` et en `en`, en 1280 px et en 380 px, clair et
sombre, sans débordement horizontal.

---

## Ce que je n'ai pas pu vérifier

- **La CI n'a jamais tourné** (`docs/STATE.md` le dit). Tout ci-dessus est local,
  sur une machine, avec un Postgres déjà chaud. Que `--frozen-lockfile`, le scan
  de secrets et ces six commandes passent sur un exécuteur GitHub n'est pas établi.
- **Aucun email réel n'est jamais parti** : pas de clé Resend. Le double de
  capture locale est le seul chemin exercé ; que `@repo/adapter-resend` transmette
  bien la locale au rendu n'est vérifié que par le typage et les tests de nœud.
- **Un seul navigateur** : Chromium. Le cookie `Secure` sur `http://localhost`
  fonctionne parce que Chromium traite `localhost` comme une origine sûre ; Safari
  et Firefox ne sont pas mesurés. Ni la persistance du choix après fermeture réelle
  du navigateur — le parcours ouvre une seconde page du même contexte.
- **Aucun déploiement** : le `307` de négociation de `/` ne porte ni `Vary` ni
  `Cache-Control`. Un `307` n'est pas mis en cache par défaut (RFC 7231), donc je
  ne le classe pas en finding — mais derrière un CDN configuré à la main, une
  réponse de langue mise en cache servirait le français à un anglophone.
- **Accessibilité** : `jsx-a11y` reste absent (dette connue). Le
  `aria-current="true"` et le `hrefLang` du `LocaleSwitcher`, et l'annonce du
  changement de `lang`, n'ont été vus par aucune technologie d'assistance.
- **Deux locales, toutes deux latines et LTR.** Aucun pluriel ICU, aucun
  `select`, aucun RTL, aucun format de date exotique n'est exercé. Le premier
  message à pluriel du dépôt sera le premier test de `unflattenMessages` sur ce
  terrain.
- **Les 2 tests ignorés** de la suite Vitest : antérieurs à s09, non instruits.
- **La recette multi-configurations de s26** n'existe pas encore : j'ai exercé
  deux états (i18n activé, i18n coupé), pas les combinaisons avec
  `demo-enabled`/`demo-disabled`.

**Gestes qu'un humain devrait faire :**

1. Écrire ``<p>{`Bonjour ${name}, bienvenue`}</p>`` dans un écran, lancer `pnpm
   test`, constater le vert — puis décider du correctif de C1 avant s10.
2. Ouvrir l'application dans Safari et dans Firefox, changer de langue, **fermer
   le navigateur**, le rouvrir, et vérifier que la langue tient (le cookie est
   `Secure` sur une origine http).
3. Déployer une préversion en HTTPS et refaire `GET /` avec
   `Accept-Language: en` puis `fr`, derrière le CDN réel.
4. Retirer `EMAIL_LOCAL_CAPTURE`, poser une vraie clé Resend, demander un lien de
   connexion depuis un navigateur anglais, et **lire l'email reçu**.

---

*Verdict de la première passe : `critical` / `no`. Conservé ici pour mémoire,
sans les deux lignes de verdict : le fichier n'en porte qu'une paire, celle de
la seconde passe ci-dessous.*

---
---

# Revue — Story s09-i18n, **seconde passe** (commit correctif)

Contexte frais, autre lecteur. Diff jugé : `git diff bd7abe0..d1e5fe7`, dont le
seul commit de code est `d1e5fe7`. Base propre au début et à la fin
(`git diff --exit-code` vert avant l'écriture de ce fichier, après restauration
de sept mutations et d'un `pnpm ks toggle i18n`).

## Ce qui a été exécuté, par moi

| Commande | État livré (i18n activé) | `pnpm ks toggle i18n` (coupé) |
|---|---|---|
| `pnpm typecheck` | vert (`--force`, 14 tâches, sans cache) | vert (`--force`) |
| `pnpm lint` | vert (`--max-warnings=0`) | vert |
| `pnpm test` | **675 passés, 2 ignorés** (25 fichiers + 1 ignoré) | **675 passés, 2 ignorés** |
| `pnpm test:e2e` | **26 passés**, deux exécutions consécutives, `retries: 0` vérifié dans `playwright.config.ts:41` | **24 passés, 2 ignorés**, deux exécutions |
| `pnpm build` | vert — arborescence **sans `[locale]`**, `ƒ Proxy (Middleware)` monté | vert — **même** arborescence |
| `pnpm run audit` | vert (1 avis, aucun au seuil « élevé » non couvert) | — |

Les chiffres annoncés sont donc exacts. Module remis en marche, arbre revérifié.

---

## 1. C1 — le filet élargi : **le défaut est réel et corrigé, la classe n'est pas fermée** → major

### Ce qui mord maintenant

J'ai extrait le détecteur dans un module à part et lui ai soumis **48 formes de
mon cru**, différentes de celles de `PLANTED`. Il voit tout ce que la première
passe lui reprochait, et davantage :

| Forme plantée | Résultat |
|---|---|
| `` <h2>{`Bienvenue sur votre espace ${account.name}`}</h2> `` | **détectée** |
| `title={'Bonjour ' + account.name}` | **détectée** |
| `<p>Bonjour {name}, bienvenue</p>` | **détectée** |
| `<p><b>Bonjour</b> tout le monde</p>` | **détectée** |
| texte JSX sur plusieurs lignes | **détectée** |
| `dangerouslySetInnerHTML={{ __html: 'Bienvenue chez vous' }}` | **détectée** |
| `` <h1>{String.raw`Bienvenue`}</h1> `` | **détectée** |
| `<Toast text="Enregistré" />`, `<Field name="Nom complet" />` | **détectées** |
| `const LABELS = { save: 'Save changes' }` | **détectée** |
| `<p>{ok ? 'Actif' : 'Inactif'}</p>` | **détectée** |
| `'Bon'+'jour'+' vous'` | **détectée** |
| `<Badge variant="secondary" />`, `cn(...)`, `Promise<Response>` | **non signalées** (bon) |

Et le défaut trouvé est **vrai** : `packages/ui/src/components/sheet.tsx` portait
`<span className="sr-only">Fermer</span>`. `RENDER_ROOTS` contenait déjà
`packages/ui/src` avant le correctif — la chaîne échappait donc bien au seul
seuil de prose, exactement comme le commit l'affirme. Vérifié.

### Ce qui échappe encore, mesuré

Un mot unique **sans accent et sans espace** qui n'est pas lexicalement dans une
position affichée reste invisible. Le conteneur peut être une variable, un
littéral d'objet, un tableau, un paramètre par défaut, ou un attribut dont le
nom n'est pas dans la liste des suffixes affichés.

Planté dans `apps/web/app/page.tsx`, **rendu à l'écran** :

```tsx
const BADGE = 'Beta'
// …
<p>{BADGE}</p>
```

→ `pnpm test` **675 verts**, `pnpm typecheck` vert, `pnpm lint` vert,
`pnpm test:e2e` **26 verts**. Les six commandes sont muettes sur une chaîne
anglaise non traduite affichée sur l'accueil.

Et la forme qui compte le plus, parce que c'est **l'idiome du dépôt** — le texte
entre dans `@repo/ui` par un objet de props (`ThemeToggle options`, `SidebarNav
items`, `LocaleSwitcher options`). Planté dans `apps/web/app/app-shell.tsx`, le
fichier même que ce commit modifie :

```tsx
options={{ light: 'Light', dark: 'Dark', system: 'System' }}
```

→ `pnpm test` **675 verts**. Seul `pnpm test:e2e` rougit, et par accident :
`e2e/app-shell.spec.ts` nomme « Sombre » et « Système » pour cliquer dessus. Un
libellé qu'aucun parcours ne nomme n'aurait rien fait rougir. C'est la même
famille que le « Fermer » que ce commit vient de fermer, une prop plus loin.

**Une troisième évasion, non déclarée, et celle-là éteint un fichier entier.**
`blankDelimited` rend `source.length - 1` quand le délimiteur ne se referme
jamais : une parenthèse non appariée à l'intérieur des arguments de `cn(` ou de
`cva(` blanchit **tout le reste du fichier**.

```tsx
const c = cn('after:content-["("]')
export const A = () => <h1>Bienvenue sur votre espace</h1>   // non détectée
```

C'est le mode d'échec n°4 du catalogue, à la granularité du fichier cette fois.

### Pourquoi major et non critical

- **Le produit livré est propre.** J'ai listé les 41 littéraux d'un seul mot que
  le détecteur voit sans les signaler, sur les 30 `.tsx` : `variant`, `submit`,
  `horizontal`, `sr-only`, noms de balises, valeurs ARIA — **aucune copie
  produit**. Le défaut est dans le filet, pas dans l'écran.
- Toutes les formes que la première passe avait mesurées vertes sont désormais
  rouges ; le filet est strictement plus large et il a payé une fois.
- La limite est **écrite dans le fichier de test** au lieu d'être tue, ce que la
  règle « ne jamais prétendre à l'exhaustivité » demande.

Mais la classe reste ouverte, et trente-six stories seront écrites contre ce
filet. **Le correctif durable n'est pas d'élargir encore l'expression
régulière** : `next-intl-swc-plugin-extractor@4.14.1` est déjà dans le magasin
de paquets, et une passe d'arbre syntaxique répond à la question
« cette chaîne atteint-elle un nœud rendu ? » que la ligne de texte ne peut pas
poser. À décider avant s10.

### Précision sur les évasions déclarées

Deux sont nommées dans `tests/i18n.test.ts` — « la concaténation de fragments
d'un seul mot sans espace mitoyenne » et « un mot unique sans accent rangé dans
une variable puis rendu par cette variable ». Les deux sont **exactes**. La
seconde est **sous-estimée** : ce n'est pas « une variable », c'est tout
conteneur non affiché, l'objet de props compris. Et le renvoi
« ce qui leur échappe encore est nommé dans `docs/reviews/s09-i18n.md` » désigne
un fichier qui, au moment du commit, ne nommait que les évasions **d'avant** le
correctif. Le présent chapitre le corrige.

**La cinquième — les `.ts` non balayés — est acceptable, et je l'ai vérifiée.**
La justification écrite (« le texte qu'ils portent est tenu ailleurs ») est
mesurée : `label: intl.t(entry.labelKey)` remplacé par `label: 'My account'`
dans `apps/web/lib/navigation.ts` → **2 tests rouges** dans
`tests/app-shell.test.ts`. Et la phrase « Ce filet ne dit donc rien des `.ts` :
c'est une limite, pas une garantie » est la formulation que le `AGENTS.md`
racine exige. Trou nommé, compensation prouvée : pas un finding.

---

## 2. `SheetContent` et le `closeLabel` obligatoire — **bon appel**

Changement d'API publique de `packages/ui`, prop **requise** et non optionnelle
avec repli. C'est le bon degré de contrainte : optionnelle, l'oubli redonnait
« Fermer » en français dans un projet anglophone, en silence — précisément le
défaut qu'on ferme. Requise, l'oubli est une erreur de compilation.

- Un seul appelant dans le dépôt (`apps/web/app/app-navigation.tsx`), câblé
  jusqu'au shell (`closeLabel={t('app.shell.closeNavigation')}`), clé livrée en
  `fr` et en `en` ;
- `packages/ui/src/components/sheet.tsx` ne porte plus aucun texte : relu en
  entier ;
- `e2e/app-shell.spec.ts` vérifie le nom accessible **dans le navigateur**, sous
  400 px, panneau ouvert ;
- `packages/ui/AGENTS.md` pose la règle « Aucun texte, jamais » et son affirmation
  « `tests/i18n.test.ts` balaie les `.tsx` de `packages/ui/src` » est vraie —
  `RENDER_ROOTS` la contient.

C'est un boilerplate, pas une bibliothèque publiée : la rupture ne coûte rien
et achète une garantie de compilation.

---

## 3. M1 — le raisonnement des deux observables : **il tient**, mais le câblage
n'est tenu par rien → major

### La partie qui tient

Les deux mutations, chacune seule, et le compte exact des rouges :

| Neutralisation dans `apps/web/i18n/request-config.ts` | Rouges |
|---|---|
| `onError: (error) => { console.error(error) }` | **1** — « ne tait pas une erreur qui n'a aucun repli de message » |
| `getMessageFallback` rendant le chemin de la clé | **1** — « refuse encore si le premier verrou venait à être desserré » |

Et la vérification que la première passe demandait : sous la mutation (a), le cas
« lève plutôt que de rendre le chemin de la clé » **reste vert** (c'est
`getMessageFallback` qui lève) ; sous la mutation (b), il reste vert aussi
(c'est `onError` qui lève en premier). Un unique test de clé manquante aurait
donc bien survécu à chacune. **Le raisonnement est exact, et les deux
observables sont les bons.** L'expression régulière `/onError:[\s\S]*?throw/` a
disparu : le comportement est exécuté, plus lu.

Le type `RequestConfig` est bien exporté par `next-intl/server` en 4.14.1
(`dist/types/server/react-server/index.d.ts`) et l'annotation de retour fait
bien rejeter un nom de gestionnaire mal orthographié (contrôle de propriété
excédentaire sur un littéral d'objet contextuellement typé). Vérifié dans le
paquet installé.

### La partie qui ne tient pas — **finding M4**

L'invariant a été déplacé là où un test l'atteint. **Rien ne prouve qu'il est
encore branché.** `apps/web/i18n/request.ts` ramené à ce qu'il était avant s09,
c'est-à-dire au repli silencieux que le critère 9 interdit :

```ts
export default getRequestConfig(async () => {
  const locale = await currentLocale()

  return { locale, messages: messagesFor(locale) }
})
```

→ `pnpm test` **675 verts**, `pnpm test:e2e` **26 verts**, `pnpm lint` vert,
`pnpm typecheck` vert. Un écran afficherait « app.account.title » sans qu'une
seule commande rougisse — le défaut exact que le critère nomme.

L'ancienne garde textuelle, avec tous ses défauts, **rougissait** sur cette
mutation-là : elle lisait `i18n/request.ts`. Le correctif a donc échangé un angle
mort contre un autre plutôt que de les fermer tous les deux.

Aggravant : `request-config.ts` documente lui-même le trou puis nomme un contrôle
qui ne le couvre pas — « Le type de retour est **annoté** : c'est le compilateur
qui vérifie que ces deux gestionnaires portent les noms que `next-intl` lit. Un
test de comportement prouve qu'ils refusent ; il ne peut pas prouver qu'ils sont
branchés. » L'annotation garde les noms **à l'intérieur** de `requestConfigFor` ;
elle ne garde pas que `request.ts` l'appelle. C'est une affirmation fausse dans
un fichier de production, sur le point précis qui manque.

Pistes, dans l'ordre où je les tenterais : rendre `currentLocale` injectable et
exercer le `default export` depuis un test de nœud ; ou une route de recette qui
cite une clé absente et qu'`e2e` attend en 500 ; à défaut, une assertion
structurelle **ancrée** (le `default export` contient l'appel à
`requestConfigFor`), qui est un pis-aller mais couvre la porte réellement
ouverte.

---

## 4. M3 — `buildRegistry({ locales })` : **clos, aux deux niveaux**

| Neutralisation | Résultat |
|---|---|
| `locales` retiré de `apps/web/lib/module-registry.ts` | **`pnpm typecheck` rouge** — `error TS2741: Property 'locales' is missing` |
| garde d'exécution de `validate.ts` remplacée par `if (false)` | **1 rouge** — « refuse un point de composition qui ne déclare pas les locales du projet » |

L'omission est donc impossible pour un appelant typé, et bruyante pour celui qui
ignore les types (le test l'atteint par un `as unknown as`, ce qui est la seule
façon honnête d'exercer cette porte). La garde refuse aussi le **tableau vide**,
et la raison écrite est juste : un ensemble vide rendrait chaque boucle de
`assertDeclarationsAreComplete` vide, donc chaque vérification verte.

Les 3 points de composition de production (`apps/web/lib/module-registry.ts`,
`packages/db/src/scripts/migrate.ts`, `packages/db/src/scripts/generate.ts`)
passent tous `[...appLocales]` — relus un à un. Les points de test le passent
explicitement, ce qui est le bon prix.

Le second paramètre de `assertDeclarationsAreComplete` devenu obligatoire : la
fonction est exportée par `packages/core/src/index.ts` mais n'a **qu'un seul
appelant** dans le dépôt (`registry.ts:assertDeclarationsAreComplete(modules,
configuration.locales)`). Rupture sans portée. Acceptable.

Limite à connaître : la garde refuse l'**absence** de locales, pas des locales
**fausses**. `locales: ['fr']` dans un projet qui sert `fr` et `en` passerait.
C'est inhérent au principe « `@repo/core` reçoit, ne lit pas », et l'ADR le
justifie ; ce n'est pas un finding, c'est une propriété à ne pas oublier.

---

## 5. M2 — le cookie `app_locale` : **clos, aux deux niveaux**

| Neutralisation | Résultat |
|---|---|
| `httpOnly: true` retiré de `apps/web/proxy.ts` | **1 rouge en nœud** (« pose le choix de langue en HttpOnly, Secure et SameSite ») **et 1 rouge en navigateur** (`e2e/i18n.spec.ts:65`) |
| `.resolves.not.toContain` inversé en `.resolves.toContain` | **rouge** — l'assertion `document.cookie` n'est pas décorative, la forme `.resolves` fonctionne bien sous Playwright |

Deux détails que j'ai vérifiés plutôt que supposés :

- `expect(cookie?.httpOnly).toBe(true)` ne peut pas passer à vide : cookie absent
  → `undefined` → rouge. Et le test de nœud porte une garde explicite
  (`if (localeRouting.locales.length > 1)`) pour ne pas être vrai sur zéro
  cookie ;
- la claim de `apps/web/AGENTS.md` (« `tests/i18n.test.ts` contrôle les en-têtes
  `Set-Cookie` que le proxy laisse partir, `e2e/i18n.spec.ts` les contrôle tels
  que le navigateur les stocke ») est exacte, et le geste est bien **le même**
  que `e2e/auth.spec.ts:368-372` sur la session.

**Sur « tout cookie hors session » : la portée est plus étroite que le socle.**
La boucle du test de nœud couvre tous les `Set-Cookie` d'**une** réponse du
proxy, pour **une** requête (`/en/account`). Un cookie posé par un gestionnaire
de route, une action serveur ou un adaptateur n'est vu par aucune commande.
`apps/web/AGENTS.md` le dit correctement (« que le proxy laisse partir ») : pas
de sur-promesse, donc pas de finding — mais la garde générique « aucun cookie du
dépôt ne part sans ses attributs » n'existe toujours pas, et c'est elle que
`docs/security.md` §1 décrit.

---

## 6. Les déviations à juger

- **`next` et `next-intl` en `devDependencies` racine** — justifié. Le précédent
  (`better-auth`, `drizzle-orm`) tient : `tests/` importe désormais
  `createTranslator`/`createFormatter` et `NextRequest`, et pnpm strict exige la
  déclaration. Versions alignées sur `apps/web/package.json` (`^16.3.0`,
  `4.14.1` épinglé). **Réserve mesurée** : le magasin porte maintenant **deux
  instances de Next 16.3.3** (`next@16.3.3_@playwright+test@…` pour la racine,
  `next@16.3.3_@types+node@…` pour `apps/web`), parce que les pairs diffèrent.
  `tests/i18n.test.ts` construit donc un `NextRequest` d'une copie et le passe à
  `proxy()` qui utilise l'autre. Cela marche aujourd'hui — le proxy ne fait que
  lire `nextUrl`, `cookies` et `headers` — et rien ne casse ; mais un
  `instanceof` ajouté un jour par Next rendrait ce test faux sans prévenir. Voir
  m8.
- **`apps/web/i18n/request-config.ts`** — le bon geste (rendre un comportement
  exécutable au lieu de le lire), avec le trou de câblage du §3.
  `apps/web/AGENTS.md` documente honnêtement pourquoi le fichier existe.
- **Les trois `AGENTS.md`** (`apps/web`, `packages/core`, `packages/ui`) — j'ai
  vérifié **chaque** affirmation mesurable qu'ils avancent ; toutes sont exactes.
  Édition non demandée mais requise par la règle « les docs voyagent avec le
  code qui les change ».
- **`docs/STATE.md`** — les deux modes d'échec ajoutés sont **exacts**, je les ai
  reproduits tous les deux : n°9 (paramètre facultatif à repli silencieux) et
  n°10 (garde qui lit le texte au lieu d'exécuter le comportement). L'amendement
  du n°4 est exact aussi. Voir m6 pour ce qui n'a pas suivi.
- **`docs/reviews/s09-i18n.md` committé par l'implémenteur** — le cycle prévoit
  que `/ks-ship` commite la revue. Ici elle est l'entrée du correctif ; la
  committer avec lui est cohérent et sans conséquence.

---

## Findings — seconde passe

### critical

Aucun.

### major

**M4 — Le critère 9 est prouvé au bon endroit, mais plus branché nulle part.**
`apps/web/i18n/request.ts` ramené à `return { locale, messages: messagesFor(locale) }`
— c'est-à-dire au repli sur le chemin de la clé que le critère interdit — laisse
`pnpm test` **675 verts**, `pnpm test:e2e` **26 verts**, `lint` et `typecheck`
verts. L'ancienne garde textuelle rougissait sur cette mutation. Et le
commentaire de `request-config.ts` nomme le compilateur comme contrôle du
câblage, ce qu'il n'est pas : l'annotation `RequestConfig` garde les noms des
gestionnaires **dans** `requestConfigFor`, pas l'appel depuis `request.ts`.
M1 est donc à moitié fermé.

**M5 (ex-C1, rétrogradé) — Le détecteur ne voit pas un mot unique rangé dans un
conteneur non affiché, ni au-delà d'un `cn(`/`cva(` mal apparié.** Mesuré :
`const BADGE = 'Beta'` + `<p>{BADGE}</p>` dans `apps/web/app/page.tsx` → **six
commandes vertes** (675 tests, 26 parcours). `options={{ light: 'Light', dark:
'Dark', system: 'System' }}` dans `apps/web/app/app-shell.tsx` → `pnpm test`
**675 verts** (l'e2e ne rougit que parce qu'il nomme ces libellés par hasard).
`cn('after:content-["("]')` blanchit tout le reste du fichier. Le produit livré
est propre — les 41 littéraux d'un seul mot des 30 `.tsx` sont tous techniques —
donc c'est le filet qui est en cause, pas l'écran ; mais s10 à s45 seront
écrites contre lui. Correctif recommandé : une passe d'arbre syntaxique
(`next-intl-swc-plugin-extractor`, déjà installé), pas une maille de plus.

### minor

**m6** — `docs/STATE.md` annonce toujours « Tests : **641 + 2 ignorés** » ; l'état
livré en compte **675**. Le commit qui ajoute ~34 tests est aussi celui qui
touche ce fichier.

**m7** — `tests/i18n.test.ts` renvoie le lecteur à `docs/reviews/s09-i18n.md`
pour « ce qui échappe encore » ; ce fichier n'y nommait que les évasions
**d'avant** le correctif. Le présent chapitre lève l'ambiguïté, mais le renvoi
mérite d'être ancré.

**m8** — Deux instances de `next@16.3.3` cohabitent (pairs différents entre la
racine et `apps/web`). `tests/i18n.test.ts` traverse la frontière avec
`NextRequest`. Sans effet aujourd'hui, fragile par construction.

**m1 de la première passe, toujours ouvert** — `packages/modules/i18n/AGENTS.md`
désigne encore `apps/web/lib/i18n.ts` comme « le montage des deux états » ; le
choix vit dans `apps/web/lib/locale-routing.ts`.

**m2 et m3 de la première passe, toujours ouverts** — aucune règle exécutable
contre un import de `@repo/module-*` depuis `apps/web/app/**` ; la dette « pas de
locale stockée » n'est écrite dans aucun `AGENTS.md`. Hors du périmètre assigné
à ce correctif, à reprendre avant s15.

---

## Ce que je n'ai pas pu vérifier

- **La CI n'a toujours jamais tourné.** Les huit commandes ci-dessus sont
  locales, sur une machine, Postgres déjà chaud. `--frozen-lockfile`, le scan de
  secrets et l'installation propre sur un exécuteur GitHub restent non établis —
  et ce commit ajoute deux dépendances racine, donc c'est justement l'exécution
  où une résolution divergente se verrait.
- **Aucun email réel** : pas de clé Resend, seul le double de capture est
  exercé.
- **Chromium seul**, et une seule fermeture de navigateur simulée (une seconde
  page du même contexte). Le `Secure` sur `http://localhost` marche parce que
  Chromium traite `localhost` comme une origine sûre ; Safari et Firefox ne sont
  pas mesurés — et c'est maintenant `HttpOnly` **plus** `Secure` qu'il faudrait
  y revoir.
- **Aucun rendu manuel dans un navigateur par moi** : je me suis appuyé sur les
  26 parcours Playwright et sur les mesures de la première passe pour l'aspect
  des écrans. Le bouton de fermeture du panneau, je l'ai vu par son nom
  accessible, pas de mes yeux.
- **La liste complète des cinq évasions déclarées** m'a été rapportée, pas
  montrée : `tests/i18n.test.ts` n'en nomme que deux, plus la limite des `.ts`.
  J'ai donc jugé le filet par la mesure — 48 formes plantées — et non par sa
  liste.
- **Aucun déploiement, aucune CSP, aucun CDN.** Le `307` de négociation ne porte
  toujours ni `Vary` ni `Cache-Control`.
- **Deux locales latines et LTR**, aucun pluriel ICU, aucun RTL.
- **Les 2 tests ignorés** de la suite Vitest : antérieurs, non instruits.

**Gestes qu'un humain devrait faire :**

1. Écrire `const BADGE = 'Beta'` puis `<p>{BADGE}</p>` dans un écran, lancer les
   six commandes, constater le vert — et **décider avant s10** entre l'extracteur
   syntaxique et l'acceptation écrite de la limite.
2. Vider le corps de `apps/web/i18n/request.ts` pour qu'il ne passe plus par
   `requestConfigFor`, relancer `pnpm test` et `pnpm test:e2e`, constater le vert :
   c'est M4, en trente secondes.
3. Ouvrir l'application dans Safari et Firefox, changer de langue, **fermer le
   navigateur**, rouvrir, vérifier que la langue tient maintenant que le cookie
   est `HttpOnly` en plus de `Secure`.
4. Faire tourner la CI **une fois**, ne serait-ce que pour cette installation
   propre avec `next` et `next-intl` à la racine.

---

Max severity: major
Ship allowed: yes
